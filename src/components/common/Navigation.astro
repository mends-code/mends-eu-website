---
// Navigation.astro
type NavLink = {
  text: string;
  href: string;
};

const navLinks: NavLink[] = Astro.props.navLinks;
---

<nav
  x-show="showNav"
  x-cloak
  x-transition:enter="transition ease-out duration-300"
  x-transition:enter-start="opacity-0 bg-opacity-0"
  x-transition:enter-end="opacity-100 bg-opacity-30"
  x-transition:leave="transition ease-in duration-300"
  x-transition:leave-start="opacity-100 bg-opacity-30"
  x-transition:leave-end="opacity-0 bg-opacity-0"
  @click="showNav = false"
  :style="navStyle"
  :class="showNav ? '' : ''"
  class="absolute overflow-auto px-10 rounded-3xl bg-hippie-blue h-auto bg-opacity-30 backdrop-blur-lg transition-opacity no-scrollbar z-50"
>
  <ul
    class="flex grow-1 flex-col h-full w-full justify-center content-center gap-4"
  >
    {
      navLinks.map((link) => (
        <li class="justify-center flex">
          <a class="py-6 px-12 text-center font-bold uppercase text-xl md:text-2xl lg:text-3xl xl:text-4xl tracking-widest align-middle text-rhino flex hover:bg-white hover:bg-opacity-70 rounded-xl transition-colors w-fit" href={link.href}>
            {link.text}
          </a>
        </li>
      ))
    }
  </ul>
</nav>
